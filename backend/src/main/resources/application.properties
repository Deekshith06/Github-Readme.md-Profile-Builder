# Run on port 8080
server.port=8080

# Allow frontend (index.html opened from file system) to call our API
# In production you'd be more specific, but for a student project this is fine
spring.mvc.cors.allowed-origins=*
